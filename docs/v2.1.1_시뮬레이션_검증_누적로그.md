# v2.1.1 시뮬레이션 검증 누적 로그

이 문서는 시뮬레이션 엔진 변경 단계마다 수행한 검증 결과를 누적 기록한다.

검증 원칙:

- 구조 변경 단계라도 검증을 생략하지 않는다.
- 수치 결과가 의도적으로 변하지 않아야 하는 단계는 이전 패턴 유지 여부를 기록한다.
- 구조 검증과 현장 비교 검증을 분리해서 기록한다.

---

## 1단계 검증. 파라미터 체계 분리

### 검증 목적

문헌 기본값과 현장 오버레이를 분리했지만, 런타임 기본 동작은 이전과 동일해야 한다.

### 수행 항목

1. 문법/구문 검증
   - `config.py`
   - `extraction_isotherm.py`
   - `single_stage.py`
   - `multistage_sx.py`
   - `validation_test.py`
   - `test_verification.py`
2. 프로필 차이 검증
3. 구조 검증 스크립트 실행
4. 현장 비교 스크립트 실행

### 결과

#### 1. 문법/구문 검증

- `compile()` 기반 syntax check 통과
- 대상 파일 6개 모두 오류 없음

#### 2. 프로필 차이 검증

`literature_default`와 `field_calibrated`의 차이는 의도한 두 항목만 확인됐다.

- `Cyanex 272 / Ni / pH50`: `5.8 -> 6.3`
- `D2EHPA / Mg / pH50`: `4.5 -> 3.2`

#### 3. `validation_test.py`

- Single stage mass balance: PASS
- Co isotherm consistency: PASS
- Multistage counter-current logic: PASS
- 다단 예제 수렴: `53 iterations`
- Co cascade balance diff: 약 `0.000050 g`

#### 4. `test_verification.py`

주요 오차 패턴은 이전 단계와 동일하게 유지됐다.

- `Data1 Ni`: `-6.397 g/L`
- `Data2 Ni`: `-2.996 g/L`
- `Data4 Li`: `+2.160 g/L`
- `Data6 Co`: `-0.845 g/L`

금속별 MAE 요약:

- `Li`: `0.826 g/L`
- `Ni`: `1.943 g/L`
- `Co`: `0.206 g/L`
- `Mn`: `0.000 g/L`
- `Ca`: `0.000 g/L`
- `Mg`: `0.000 g/L`
- `Zn`: `0.000 g/L`

### 결론

1단계 변경은 의도대로 `구조 정리만 수행`했고, 기본 시뮬레이션 동작과 기존 검증 패턴은 유지됐다.

즉 이번 단계는 다음 정확도 개선 작업을 위한 기반 정리로 판단할 수 있다.

---

## 2단계 검증. 황산염 착물/자유 금속 speciation 연결

### 검증 목적

`K_MSO4`가 실제 speciation 계산에 연결되었는지 확인하고, 기존 시뮬레이션 결과를 불필요하게 깨지 않는지 확인한다.

### 수행 항목

1. 문법/구문 검증
2. speciation helper 직접 호출 확인
3. `validation_test.py`
4. `test_verification.py`

### 결과

#### 1. 문법/구문 검증

- `compile()` 기반 syntax check 통과
- 대상 파일 6개 모두 오류 없음

#### 2. speciation helper 직접 확인

조건:

- 금속: `Co`
- `pH = 5.5`
- `C_sulfate = 1.5 M`

결과:

- `free_metal_fraction = 0.002903`
- `hydroxo_fraction = 0.000000` 수준
- `sulfate_fraction = 0.997097`
- `free_sulfate_m = 1.499537`

해석:

- 고황산염 조건에서 Co는 대부분 sulfate complex 형태로 존재하도록 계산된다.
- 따라서 hydrolysis 기여가 매우 작아지고, `K_MSO4`가 실제 speciation 계산에 반영됨을 확인했다.

#### 3. `validation_test.py`

- Single stage mass balance: PASS
- Co isotherm consistency: PASS
- Multistage counter-current logic: PASS
- 다단 예제 수렴: `53 iterations`
- Co cascade balance diff: 약 `0.000050 g`

#### 4. `test_verification.py`

주요 후액 오차 패턴은 이전 단계와 동일하게 유지됐다.

- `Data1 Ni`: `-6.397 g/L`
- `Data2 Ni`: `-2.996 g/L`
- `Data4 Li`: `+2.160 g/L`
- `Data6 Co`: `-0.845 g/L`

금속별 MAE 요약도 유지:

- `Li`: `0.826 g/L`
- `Ni`: `1.943 g/L`
- `Co`: `0.206 g/L`
- `Mn`: `0.000 g/L`
- `Ca`: `0.000 g/L`
- `Mg`: `0.000 g/L`
- `Zn`: `0.000 g/L`

### 결론

2단계는 의도대로 `K_MSO4`를 실제 speciation 계산에 연결했고, 기존 현장 비교 패턴은 깨지지 않았다.

다만 이번 단계는 `pH/speciation submodel` 개선에 가깝기 때문에, 후액 농도 정확도 자체는 아직 크게 달라지지 않았다.

즉 다음 단계에서는 `free metal`을 추출 계산 쪽에 어떻게 연결할지 더 직접적으로 다뤄야 한다.

---

## 3단계 검증. D2EHPA-Co 고황산염 상대 보정

### 검증 목적

고황산염 D2EHPA 조건에서 Co 과추출을 줄이되, Ni나 전체 수지 검증을 망치지 않는지 확인한다.

### 수행 항목

1. 문법/구문 검증
2. sulfate D 보정 계수 직접 확인
3. `validation_test.py`
4. `test_verification.py`

### 결과

#### 1. 문법/구문 검증

- `compile()` 기반 syntax check 통과
- 대상 파일 6개 모두 오류 없음

#### 2. sulfate D 보정 계수 직접 확인

- `D2EHPA / Co / sulfate 0.7 M`: `1.0`
- `D2EHPA / Co / sulfate 1.0 M`: `1.0`
- `D2EHPA / Co / sulfate 1.5 M`: `0.316406`
- `Cyanex 272 / Co / sulfate 1.5 M`: `1.0`
- `D2EHPA / Ni / sulfate 1.5 M`: `1.0`

해석:

- 이번 단계 보정은 의도대로 `D2EHPA-Co` 조합에만 작동한다.
- 검증 범위의 저/중 sulfate 조건에서는 거의 영향이 없고, 고황산염 쪽만 선택적으로 억제한다.

#### 3. `validation_test.py`

- Single stage mass balance: PASS
- Co isotherm consistency: PASS
- Multistage counter-current logic: PASS
- 다단 예제 수렴: `53 iterations`
- Co cascade balance diff: 약 `0.000050 g`

#### 4. `test_verification.py`

주요 개선:

- `Data4 Co`: `-0.230 -> -0.130 g/L`
- `Data6 Co`: `-0.845 -> -0.070 g/L`
- `Data4 Ni`: `+1.331 -> -0.327 g/L`
- `Data6 Ni`: `+0.769 -> -0.852 g/L`

변화가 거의 없는 항목:

- `Data1 Ni`: `-6.397 g/L` 유지
- `Data2 Ni`: `-2.996 g/L` 유지
- `Data5 Co`: `-0.160 -> -0.158 g/L`

금속별 MAE:

- `Li`: `0.820 g/L`  (기존 `0.826`)
- `Ni`: `1.792 g/L`  (기존 `1.943`)
- `Co`: `0.060 g/L`  (기존 `0.206`)
- `Mn`: `0.000 g/L`
- `Ca`: `0.000 g/L`
- `Mg`: `0.000 g/L`
- `Zn`: `0.000 g/L`

### 결론

3단계는 broad sulfate correction이 아니라 `D2EHPA-Co`에 한정한 선택 보정으로 정리했고, 그 결과 검증상 유의미한 개선을 확인했다.

특히 Co MAE가 크게 줄었고, 고황산염 D2EHPA 조건에서 Ni도 일부 동반 개선됐다.

다만 Cyanex 272 조건의 저 pH Ni 과추출 문제는 그대로 남아 있으므로, 다음 단계는 Ni 쪽 현장 재보정 또는 목표 pH 구조 보정으로 이어가는 편이 맞다.

---

## 4단계 검증. Cyanex 272-Ni 현장 재보정

### 검증 목적

`Cyanex 272 / Ni`의 현장 오버레이를 재보정했을 때, `Data1/2`의 저 pH Ni 과추출이 줄고 `Data3` 및 기존 구조 검증이 유지되는지 확인한다.

### 수행 항목

1. 문법/구문 검증
2. 후보 파라미터 grid 확인
3. `validation_test.py`
4. `test_verification.py`

### 결과

#### 1. 문법/구문 검증

- `compile()` 기반 syntax check 통과
- 대상 파일 6개 모두 오류 없음

#### 2. 후보 파라미터 grid 확인

사전 비교 결과, `pH50` 단독 이동보다 `pH50 + k` 동시 재보정이 `Data1/2` 균형에 더 유리했다.

최종 채택값:

- `Cyanex 272 / Ni / pH50 = 6.55`
- `Cyanex 272 / Ni / k = 2.0`

대표 비교:

- `pH50=6.55, k=2.0`
  - `Data1 Ni`: `-1.945 g/L`
  - `Data2 Ni`: `+0.954 g/L`
  - `Data3 Ni`: `-0.044 g/L`
- `pH50=6.3, k=2.5` (기존)
  - `Data1 Ni`: `-6.397 g/L`
  - `Data2 Ni`: `-2.996 g/L`
  - `Data3 Ni`: `-0.044 g/L`

#### 3. `validation_test.py`

- Single stage mass balance: PASS
- Co isotherm consistency: PASS
- Multistage counter-current logic: PASS
- 다단 예제 수렴: `53 iterations`
- Co cascade balance diff: 약 `0.000050 g`

#### 4. `test_verification.py`

주요 변화:

- `Data1 Ni`: `-6.397 -> -1.945 g/L`
- `Data2 Ni`: `-2.996 -> +0.954 g/L`
- `Data3 Ni`: `-0.044 g/L` 유지

기타 주요 항목:

- `Data4 Li`: `+2.143 g/L`
- `Data4 Co`: `-0.130 g/L`
- `Data6 Ni`: `-0.852 g/L`
- `Data6 Co`: `-0.070 g/L`

금속별 MAE:

- `Li`: `0.816 g/L`  (기존 `0.820`)
- `Ni`: `0.709 g/L`  (기존 `1.792`)
- `Co`: `0.060 g/L`  (유지)
- `Mn`: `0.000 g/L`
- `Ca`: `0.000 g/L`
- `Mg`: `0.000 g/L`
- `Zn`: `0.000 g/L`

### 결론

4단계는 별도 임시 규칙 없이 `Cyanex 272 / Ni` 현장 프로필 재보정만으로 가장 큰 잔여 오차 구간을 의미 있게 줄였다.

특히 `Ni MAE`가 크게 감소했고, `Data3`와 기존 구조 검증은 유지됐다.

즉 이번 단계는 `선택 보정 로직 추가`보다 `site parameter 재보정`이 더 효과적이었던 사례로 정리할 수 있다.

---

## 5단계 검증. 목표 pH 모드 NaOH 희석 self-consistent 계산

### 검증 목적

선택적으로 추가한 `target pH + C_NaOH` 경로가 아래 두 조건을 만족하는지 확인한다.

1. 새 경로를 사용할 때 `fixed NaOH` 계산과 수치적으로 일관되어야 한다.
2. 기존 field verification 흐름은 기본 OFF 상태에서 그대로 유지되어야 한다.

### 수행 항목

1. 문법/구문 검증
2. `validation_test.py` 신규 synthetic consistency check 포함 실행
3. `test_verification.py` 실행
4. Streamlit 앱 부팅 확인

### 결과

#### 1. 문법/구문 검증

- `compile()` 기반 syntax check 통과
- 대상 파일 8개 모두 오류 없음

#### 2. `validation_test.py`

기존 Step 1~3은 그대로 PASS.

신규 `Step 4: Target pH Self-Consistent Dilution Check` 결과:

- 가정 `NaOH 농도 = 5.0 M`
- 자동 역산 `NaOH 유량 = 4.0427 L/hr`
- target 모드 희석 반복 횟수: `15`
- 동일 유량을 `fixed NaOH`에 재주입했을 때:
  - `pH_out = 4.5001`
  - 목표 pH `4.5000`
  - 차이 `0.000100`

target-vs-fixed 후액 차이:

- `Li`: `0.000000 g/L`
- `Ni`: `0.000004 g/L`
- `Co`: `0.000067 g/L`
- `Mn`: `0.000011 g/L`

즉 새 target pH 희석 경로는 고정 NaOH 계산과 사실상 동일한 해를 재현했다.

추가 multistage smoke check:

- 3단 예제 수렴: `49 iterations`
- `NaOH_flow_profile = [3.9227, 0.4128, 0.1428] L/hr`
- `pH_profile = [4.5, 4.5, 4.5]`

즉 stage별 자동 NaOH 유량이 다음 stage 수계 유량에 정상 누적되는 것도 확인했다.

#### 3. `test_verification.py`

이번 단계는 기본 동작을 유지하도록 설계했기 때문에 field verification 결과는 4단계와 동일하게 유지됐다.

주요 항목:

- `Data1 Ni`: `-1.945 g/L`
- `Data2 Ni`: `+0.954 g/L`
- `Data3 Ni`: `-0.044 g/L`
- `Data4 Li`: `+2.143 g/L`
- `Data6 Co`: `-0.070 g/L`

금속별 MAE:

- `Li`: `0.816 g/L`
- `Ni`: `0.709 g/L`
- `Co`: `0.060 g/L`
- `Mn`: `0.000 g/L`
- `Ca`: `0.000 g/L`
- `Mg`: `0.000 g/L`
- `Zn`: `0.000 g/L`

#### 4. Streamlit 부팅 확인

- `http://localhost:8765` 정상 기동 확인

### 결론

5단계는 field MAE를 직접 바꾸는 단계가 아니라, 목표 pH 모드에 선택적 희석 self-consistent 계산 경로를 추가한 단계다.

검증상 새 경로는 `fixed NaOH` 해와 정합적으로 동작했고, 기존 현장 검증 패턴도 유지됐다.

즉 이번 단계는 **정확도 개선을 위한 기반 확장**으로 판단할 수 있다.

---

## 6단계 검증. 현장 검증 데이터 basis 재구성

### 검증 목적

이번 단계는 `legacy premixed`와 `raw feed + assumed NaOH`를 분리해 검증하는 인프라를 만드는 것이 목적이다.

따라서 아래 세 가지를 확인한다.

1. sulfate 희석 전파를 넣어도 기존 구조 검증이 유지되는가
2. legacy premixed basis 결과가 기존과 동일한가
3. raw-feed basis에서 어떤 오차가 커지는지, 그리고 그 원인이 NaOH 가정과 얼마나 관련 있는지 드러나는가

### 수행 항목

1. 문법/구문 검증
2. `validation_test.py`
3. `test_verification.py`의 두 basis 실행
4. Streamlit 앱 부팅 확인

### 결과

#### 1. 문법/구문 검증

- `compile()` 기반 syntax check 통과
- 대상 파일 9개 모두 오류 없음

#### 2. `validation_test.py`

기존 Step 1~4 모두 PASS.

특히 `Step 4: Target pH Self-Consistent Dilution Check`는 sulfate 희석 전파를 넣은 뒤에도 유지됐다.

- 자동 역산 `NaOH 유량 = 4.0439 L/hr`
- fixed NaOH 재주입 pH 차이 `0.000100`
- target-vs-fixed 후액 차이:
  - `Li`: `0.000000 g/L`
  - `Ni`: `0.000004 g/L`
  - `Co`: `0.000067 g/L`
  - `Mn`: `0.000011 g/L`

#### 3. `test_verification.py`

이제 스크립트는 두 basis를 모두 출력한다.

##### A. `legacy_premixed_target_pH`

기존 주 검증 경로는 그대로 유지됐다.

금속별 MAE:

- `Li`: `0.816 g/L`
- `Ni`: `0.709 g/L`
- `Co`: `0.060 g/L`
- `Mn`: `0.000 g/L`
- `Ca`: `0.000 g/L`
- `Mg`: `0.000 g/L`
- `Zn`: `0.000 g/L`

즉 6단계는 legacy premixed 기준 성능을 의도적으로 바꾸지 않았다.

##### B. `raw_feed_target_pH`

가정:

- `NaOH 농도 = 5.0 M`

금속별 MAE:

- `Li`: `0.619 g/L`
- `Ni`: `2.261 g/L`
- `Co`: `0.086 g/L`
- `Mn`: `0.000 g/L`
- `Ca`: `0.000 g/L`
- `Mg`: `0.000 g/L`
- `Zn`: `0.000 g/L`

주요 패턴:

- `Data1 Ni`: `-4.734 g/L`
- `Data4 Ni`: `-3.124 g/L`
- `Data6 Ni`: `-3.628 g/L`

반면 Li는 일부 개선됐다.

NaOH 유량 비교:

- `Data1`: listed `11.000`, estimated `10.977 L/hr`
- `Data2`: listed `9.400`, estimated `8.091 L/hr`
- `Data3`: listed `9.400`, estimated `33.655 L/hr`
- `Data4`: listed `4.200`, estimated `10.904 L/hr`
- `Data5`: listed `3.200`, estimated `12.557 L/hr`
- `Data6`: listed `3.500`, estimated `8.760 L/hr`

해석:

- CoSX 일부 조건(Data1)는 5 M 가정이 그럭저럭 맞지만,
- Data3~6은 listed `naoh_flow`와 크게 어긋난다.
- 즉 raw-feed basis의 오차 증가는 현재 모델이 나빠졌다기보다, **실제 NaOH 농도 메타데이터가 없어서 5 M 가정이 dataset과 맞지 않기 때문**일 가능성이 높다.

#### 4. Streamlit 부팅 확인

- `http://localhost:8765` 정상 기동 확인

### 결론

6단계는 MAE를 직접 개선한 단계가 아니라, **검증 기준을 물리 basis별로 분리**한 단계다.

중요한 결론은 다음과 같다.

- 현재 주 검증 기준은 여전히 `legacy premixed`가 더 안정적이다.
- raw-feed basis는 이미 엔진상 지원되지만, Data1~6에 실제 NaOH 농도 정보가 없어 아직 preview 수준이다.
- 따라서 다음 단계에서 회귀 테스트를 만든다면, 우선 legacy premixed를 공식 회귀 기준으로 고정하고, raw-feed는 보조 진단 기준으로 두는 편이 합리적이다.
