# Scripts

- Put one-off analysis and comparison scripts in `scripts/analysis/`.
- Put new verification helpers in `scripts/verification/`.
- Keep root-level compatibility scripts only when existing commands, docs, or automation already depend on them.
